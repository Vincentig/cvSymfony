{% extends ":admin:index.html.twig" %}

{% block contenu %}
    {{   parent() }}
    <h1>Skill edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_skill') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
